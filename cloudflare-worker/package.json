{
  "name": "email-scheduler",
  "version": "1.0.0",
  "description": "Cloudflare Worker to schedule and send birthday emails.",
  "main": "src/index.js",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "cfengli007",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}